# === Node.js ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
.pnpm-debug.log

# Optional: if you're using build tools
dist/
build/
.cache/
.next/
.out/
.env
.env.*

# === Terraform ===
*.tfstate
*.tfstate.*
.crash
.terraform/
.terraform.lock.hcl
terraform.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local-only backend files (if using S3 or remote state)
.terraform.tfstate.backup
terraform.tfstate.backup

# === Docker ===
*.tar
*.img
*.log
docker-compose.override.yml

# === Kubeconfig files ===
kubeconfig
kubeconfig.yaml
.kube/
.kube/config

# === kubectl cache and plugin data ===
~/.kube/cache/
~/.kube/http-cache/
~/.kube/plugins/

# === Kustomize build output ===
kustomization.yaml.bak
kustomization.yaml.tmp
*.kustomize.config

# === Helm-related files ===
*.tgz
charts/
Chart.lock
values.override.yaml
values.local.yaml


.old_git